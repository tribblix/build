#!/bin/sh
#
# fix script run in the install context
# assumes you're in the directory containing the built software
# the argument is DESTDIR
#

case $# in
1)
	DESTDIR=$1
	;;
*)
	exit 0
	;;
esac
PREFIX=usr/versions/openssl-3

if [ ! -d "${DESTDIR}" ]; then
   exit 0
fi

case `uname -p` in
sparc)
  ARCH64="sparcv9"
  ;;
i386)
  ARCH64="amd64"
  ;;
esac

#
# local package tweaks below
#
# save the 64-bit configuration.h; the fixit script will sort it out
#

mv ${DESTDIR}/${PREFIX}/include/openssl/configuration.h ${DESTDIR}/${PREFIX}/include/openssl/configuration-64.h

#
# save a 64-bit openssl binary
#
mkdir -p  ${DESTDIR}/${PREFIX}/bin/${ARCH64}
mv ${DESTDIR}/${PREFIX}/bin/openssl ${DESTDIR}/${PREFIX}/bin/${ARCH64}/openssl
