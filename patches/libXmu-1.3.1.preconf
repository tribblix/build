#!/bin/sh
#
# SPDX-License-Identifier: CDDL-1.0
#
cp -r ${PATCHDIR}/libXmu/* .
cp ${PATCHDIR}/mapfile-Xmu .
cp ${PATCHDIR}/mapfile-Xmuu .
#
# we may need to do this because of automake and libtool
# version mismatches
#
#autoreconf -fvi
