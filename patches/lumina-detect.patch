--- lumina-1.6.2-old/src-qt5/OS-detect.pri~	Sun Dec 26 02:33:45 2021
+++ lumina-1.6.2-old/src-qt5/OS-detect.pri	Sun Jan 30 11:41:58 2022
@@ -57,6 +57,11 @@
     LIBS += -L/usr/local/lib -L/usr/lib
     #Use the defaults for everything else
 
+  }else : equals(QMAKE_HOST.os, "SunOS"){
+    isEmpty(PREFIX){ PREFIX=/usr }
+    OS = SunOS
+    #Use the defaults for everything else
+
   }else : linux-*{
     isEmpty(PREFIX){ PREFIX=/usr }
     L_SESSDIR=/usr/share/xsessions
