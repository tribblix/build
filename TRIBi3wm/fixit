#!/bin/sh
#
# tweak the default terminal preferences
# we don't ship a x-terminal-emulator link like debian, so replace
# that with our preferred default of sakura
#
sed -i s:x-terminal-emulator:sakura: usr/bin/i3-sensible-terminal
